import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function CartMeal({data}) {
  return (
    <View>
      <Text>CartMeal</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
